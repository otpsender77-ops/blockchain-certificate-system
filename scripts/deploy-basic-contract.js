const { Web3 } = require('web3');
const fs = require('fs');
const path = require('path');
require('dotenv').config({ path: './config.env' });

async function deployBasicContract() {
  try {
    console.log('🚀 Starting basic contract deployment...');
    
    // Connect to Ganache
    const ganacheUrl = `http://${process.env.GANACHE_HOST}:${process.env.GANACHE_PORT}`;
    const web3 = new Web3(ganacheUrl);
    
    // Verify connection
    try {
      await web3.eth.getBlockNumber();
      console.log('✅ Connected to Ganache blockchain');
    } catch (error) {
      throw new Error('Cannot connect to Ganache blockchain');
    }

    // Get accounts
    const accounts = await web3.eth.getAccounts();
    console.log(`📋 Found ${accounts.length} accounts in Ganache`);
    
    if (accounts.length === 0) {
      throw new Error('No accounts found in Ganache');
    }

    // Set default account
    const defaultAccount = accounts[0];
    web3.eth.defaultAccount = defaultAccount;
    console.log(`🔑 Using account: ${defaultAccount}`);

    // Deploy a very basic contract
    console.log('📝 Deploying basic contract...');
    
    // Use a minimal contract bytecode that just returns 0
    const basicContractBytecode = '...';

    // Deploy using raw transaction
    const deployTx = {
      from: defaultAccount,
      data: basicContractBytecode,
      gas: 100000,
      gasPrice: '20000000000'
    };

    console.log(`⛽ Using gas limit: ${deployTx.gas}`);

    // Send deployment transaction
    const receipt = await web3.eth.sendTransaction(deployTx);
    const contractAddress = receipt.contractAddress;
    
    console.log(`✅ Basic contract deployed at address: ${contractAddress}`);

    // Save contract address to environment file
    const envPath = path.join(__dirname, '../config.env');
    let envContent = fs.readFileSync(envPath, 'utf8');
    
    // Remove existing CONTRACT_ADDRESS if it exists
    envContent = envContent.replace(/CONTRACT_ADDRESS=.*\n/g, '');
    
    // Add new contract address
    envContent = envContent.replace(
      /# Smart Contract Configuration\nCONTRACT_ADDRESS=/,
      `# Smart Contract Configuration\nCONTRACT_ADDRESS=${contractAddress}`
    );
    
    fs.writeFileSync(envPath, envContent);
    console.log('💾 Basic contract address saved to config.env');

    console.log('🎉 Basic contract deployment completed successfully!');
    
    return {
      contractAddress,
      web3
    };

  } catch (error) {
    console.error('❌ Basic contract deployment failed:', error);
    throw error;
  }
}

// Run deployment if this script is executed directly
if (require.main === module) {
  deployBasicContract()
    .then(() => {
      console.log('✅ Basic deployment script completed');
      process.exit(0);
    })
    .catch((error) => {
      console.error('❌ Basic deployment script failed:', error);
      process.exit(1);
    });
}

module.exports = { deployBasicContract };
